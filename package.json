{
    "dependencies" : {
        "argparse" : "*",
        "npmlog" : "0.0.6",
        "node-etcd" : "2.0.8",
        "js-yaml" : "3.0.1",
        "node-uuid" : "1.4.1",
        "aws-sdk" : "2.0.0-rc9"
    },
    "devDependencies" : {
        "istanbul" : "0.2.3",
        "mocha" : "1.17.1"
    },
    "scripts" : {
        "coverage" : "rm -fr build/coverage && mkdir -p build/coverage && istanbul cover --dir build/coverage _mocha -- --reporter spec --recursive test/disco",
        "test" : "mocha --reporter spec --recursive test/disco"
    }
}
